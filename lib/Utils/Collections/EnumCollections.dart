enum PageStatus{
  initiating,
  loading,
  ready
}

enum KeyboardAction{
  hint,
  erase,
  submit
}

enum FlavorEnvironment{
  debug,
  release
}

enum HintState{
  loading,
  ready,
  failed
}

enum SettingList{
  mainMenu,
  credit,
  privacyPolicy
}

enum MusicState{
  none,
  landingPage,
  stage
}