enum PageStatus{
  initiating,
  loading,
  ready,
  error
}

enum KeyboardAction{
  hint,
  erase,
  submit
}

enum FlavorEnvironment{
  debug,
  release
}

enum HintState{
  loading,
  ready,
  failed
}

enum SettingList{
  mainMenu,
  privacyPolicy,
  about
}

enum MusicState{
  none,
  landingPage,
  stage
}

enum LevelIconSimbol{
  closed,
  current,
  open
}